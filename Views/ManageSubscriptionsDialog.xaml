<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="LinqqXrayVPN.Views.ManageSubscriptionsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:LinqqXrayVPN.Models"
             xmlns:local="using:LinqqXrayVPN.Views"
             x:Name="ThisDialog"
             mc:Ignorable="d"
             Width="360">

    <StackPanel Spacing="16"
                Margin="0,4,0,0">
        <Grid ColumnDefinitions="*,Auto"
              ColumnSpacing="12">
            <TextBlock Grid.Column="0"
                       Text="{x:Bind ViewModel.DialogTitle, Mode=OneWay}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />

            <controls:Segmented Grid.Column="1"
                                HorizontalAlignment="Right"
                                SelectedIndex="{x:Bind ViewModel.SelectedIndex, Mode=TwoWay}">
                <controls:Segmented.Resources>
                    <SolidColorBrush x:Key="SegmentedItemBackgroundPointerOver" Color="Transparent" />
                    <SolidColorBrush x:Key="SegmentedItemBorderBrushPointerOver" Color="Transparent" />
                </controls:Segmented.Resources>
                <controls:SegmentedItem 
                                        Padding="0"
                                        ToolTipService.ToolTip="{x:Bind Loc.GetString('set5.1'), Mode=OneWay}">
                    <controls:SegmentedItem.Icon>
                        <FontIcon Glyph="&#xED0E;" FontSize="14" />
                    </controls:SegmentedItem.Icon>
                </controls:SegmentedItem>
                <controls:SegmentedItem 
                                        Padding="0"
                                        ToolTipService.ToolTip="{x:Bind Loc.GetString('set5.2'), Mode=OneWay}">
                    <controls:SegmentedItem.Icon>
                        <FontIcon  Glyph="&#xEBC3;" FontSize="14" />
                    </controls:SegmentedItem.Icon>
                </controls:SegmentedItem>
            </controls:Segmented>
        </Grid>

        <StackPanel Spacing="14"
                    Visibility="{x:Bind ViewModel.AddPageVisibility, Mode=OneWay}">
            <TextBox Header="{x:Bind Loc.GetString('set5.3'), Mode=OneWay}"
                     PlaceholderText="https://..."
                     TextWrapping="Wrap"
                     Text="{x:Bind ViewModel.SubscriptionUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="{x:Bind Loc.GetString('set5.4'), Mode=OneWay}"
                     PlaceholderText="{x:Bind Loc.GetString('set5.5'), Mode=OneWay}"
                     Text="{x:Bind ViewModel.SubscriptionName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="{x:Bind Loc.GetString('set5.6'), Mode=OneWay}"
                       FontSize="12"
                       Opacity="0.65" />
        </StackPanel>

        <Grid Visibility="{x:Bind ViewModel.ManagePageVisibility, Mode=OneWay}">
            <TextBlock Text="{x:Bind Loc.GetString('set5.7'), Mode=OneWay}"
                       HorizontalAlignment="Center"
                       Margin="0,48,0,48"
                       FontSize="14"
                       Opacity="0.6"
                       Visibility="{x:Bind ViewModel.EmptyStateVisibility, Mode=OneWay}" />

            <ScrollViewer MaxHeight="440"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Visibility="{x:Bind ViewModel.ListVisibility, Mode=OneWay}">
                <StackPanel x:Name="SubscriptionsPanel"
                            Spacing="8" />
            </ScrollViewer>
        </Grid>
    </StackPanel>
</UserControl>