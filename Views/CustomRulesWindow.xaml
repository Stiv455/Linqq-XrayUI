<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="LinqqXrayVPN.Views.CustomRulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="using:LinqqXrayVPN.Models"
        mc:Ignorable="d"
        Title="{x:Bind Loc.GetString('set4.1'), Mode=OneWay}">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Padding="20,20,20,12"
              ColumnDefinitions="*,Auto">
            <TextBlock Grid.Column="0"
                       Text="{x:Bind Loc.GetString('set4.2'), Mode=OneWay}"
                       Style="{ThemeResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="8">
                <Button x:Name="UpdateGeoButton"
                        Padding="6"
                        Background="Transparent"
                        BorderThickness="0"
                        VerticalAlignment="Center"
                        ToolTipService.ToolTip="{x:Bind Loc.GetString('set4.3'), Mode=OneWay}"
                        Click="UpdateGeoButton_Click">
                    <FontIcon Glyph="&#xE895;" FontSize="14" />
                </Button>
                <Button Style="{ThemeResource AccentButtonStyle}"
                        Command="{x:Bind ViewModel.AddRuleCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="{x:Bind Loc.GetString('add'), Mode=OneWay}" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2"
                      Margin="20,0,20,0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="RulesPanel"
                        Spacing="4"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="8" />
        </ScrollViewer>

        <Border Grid.Row="3"
                BorderThickness="0,1,0,0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                Padding="20,16">
            <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        HorizontalAlignment="Right">
                <Button Style="{ThemeResource AccentButtonStyle}"
                        MinWidth="80"
                        Content="{x:Bind Loc.GetString('set4.5'), Mode=OneWay}"
                        Command="{x:Bind ViewModel.SaveCommand}" />
                <Button MinWidth="80"
                        Content="{x:Bind Loc.GetString('set4.6'), Mode=OneWay}"
                        Command="{x:Bind ViewModel.CancelCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>